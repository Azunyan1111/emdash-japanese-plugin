import type { CatalogEntry } from "../../../types.js";

const entries: CatalogEntry[] = [
	{
		"source": "w",
		"target": "w",
		"occurrences": [
			{
				"file": "admin/src/lib/url.ts",
				"line": 39,
				"kind": "literal"
			}
		]
	}
] as CatalogEntry[];

export default entries;
